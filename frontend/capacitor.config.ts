import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.moodevaluationapp.app',
  appName: 'MoodEvaluationApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
